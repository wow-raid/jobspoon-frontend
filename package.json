{
  "name": "root",
  "private": true,
  "workspaces": [
    "main-container",
    "navigation-bar-app",
    "vue-account-app"
  ],
  "scripts": {
    "build": "lerna run build --parallel",
    "start": "lerna run start --parallel"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "external-remotes-plugin": "^1.0.0",
    "lerna": "^8.2.1"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
